#include "header.h"

extern double minimum(double a,double b);

double func1(double M,double b, double d, double alpha, double beta, double gamma){

	double temp;

	temp= pow(M,-2.0)+pow(b,-2.0);

	return temp;

}